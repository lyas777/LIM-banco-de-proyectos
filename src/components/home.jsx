import React from 'react';


const Home = () => {
  // const [flipped , setFlipped] = useState([]);
  // const handleClick = (id) => setFlipped([...flipped, id]);

    return(
      <main>
        <header>

        </header>

      </main>
    );

}

export default Home;
